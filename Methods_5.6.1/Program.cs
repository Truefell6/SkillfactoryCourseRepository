using System;

class Program
{
    static void Main(string[] args) //метод, который заполняет данные с клавиатуры по пользователю (возвращает кортеж)
    {
        Console.WriteLine("Напишите что-то");
        Console.ReadKey();
    }

    static void IsPositiveNumber() //метод, который проверяет, является ли введённое значение положительным числом
    {
        Console.WriteLine();
    }

    static void GetArrayWithKnownLength() //метод, который заполняет данные с клавиатуры с заранее известной длиной массива
    {
        Console.WriteLine();
    }

    static void ShowUserData() //метод, который показывает введённые данные
    {
        Console.WriteLine();
    }
}